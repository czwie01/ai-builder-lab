---
description: Grade the current diff against the practice's acceptance criteria
---

Follow the tool-agnostic rubric in `docs/workflow/practice-review.md` against the current
diff, using the relevant spec in `docs/practices/` and the rules in `AGENTS.md`. Output the
scored table and verdict exactly as the rubric specifies.
